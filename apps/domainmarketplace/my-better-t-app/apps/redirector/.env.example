# Environment variables for the redirector worker

# Neon Postgres HTTP connection string used by the RouteCoordinator Durable Object
DATABASE_URL=https://example-user:example-password@ep-example-pool.neon.tech/neondb

# Configure bindings in wrangler.jsonc; use wrangler secret put DATABASE_URL for production.
# KV namespace IDs and Analytics Engine datasets are defined via wrangler CLI and not stored in .env.

